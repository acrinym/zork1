# Attic Playback — Release 1227

## Qualified product

Release `1227` extends qualified Release `1226` with bounded, period-authentic playback of meaningful events already earned by this expedition.

The canonical Attic gains seven exact physical records:

- `PLAYBACK-PRINTER-01` — continuous-feed curated transcript;
- `PLAYBACK-CASSETTE-02` — textual cassette with speaker labels, pauses, hiss, and environmental cues;
- `PLAYBACK-INCIDENT-03` — incident scene index;
- `PLAYBACK-ACTOR-04` — actor scene index;
- `PLAYBACK-PLACE-05` — place scene index;
- `PLAYBACK-CHRONOLOGY-06` — deterministic first-capture order;
- `PLAYBACK-FORENSIC-07` — bounded twelve-event forensic strip.

## What is captured

The recorder observes only consequential state already established by the canonical game and earlier qualified layers:

- house return and Attic use;
- individually earned forest evidence;
- Dam diagnosis, refused or failed interactions, and repair;
- Hades ceremony stages, wrong-order consequences, and completion;
- player-specific troll, cyclops, and thief evidence;
- complete regional synthesis.

Each event is recorded once. Ordinary movement, parser chatter, typos, repeated observations, and unseen events are not stored.

## Curated, not omniscient

Printer echoes are normalized labels such as `[CURATED ACTION] EXAMINE THE DAM CONTROL PANEL`; they are not represented as the player's exact historical wording.

Cassette speech is original contextual paraphrase, not invented verbatim dialogue. Missing scenes remain absent.

## No time travel

Playback review is non-turning for threat and timer machinery. Every guarded read snapshots and verifies:

- location;
- score;
- active Shadow threat timer;
- troll, cyclops, and thief locations;
- parser pronoun;
- physical record custody.

Playback may print text and mark that the archive was reviewed. It cannot replay actors, move objects, change score, open routes, advance timers, or repair evidence.

## Capacity and persistence

The sequence stores at most twelve unique consequential events. Native save state retains the capture mask, deterministic sequence, indexes, filing state, prior parser pronoun, and exact physical custody.

GitHub Actions run `30493076701` passed:

- exact Release `1226` base and five-path staging;
- locked Release `1227` artifact identity;
- production smoke rejecting unearned Dam, Hades, actor, synthesis, and forensic playback;
- all seven parser-facing records;
- twelve-event capacity and deduplication;
- incident, actor, place, and chronology navigation;
- active-timer, score, actor, pronoun, location, and custody integrity;
- exact filing;
- native `SAVE`, deliberate state/media corruption, and `RESTORE` without post-restore repair;
- production/test isolation.

## Locked identity

- Release: `1227`
- Serial: `260729`
- File: `zork1-glulx-attic-playback.ulx`
- Size: `307,712` bytes
- Checksum: `0xfb794f11`
- SHA-256: `6146311cd1fab20c5fde50f12a569c3ea9b34fd0f42038448f44f3740b9936f0`
